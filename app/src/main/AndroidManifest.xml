<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.theholidayapp.project">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Project">
        <activity android:name=".offerpetaluma"></activity>
        <activity android:name=".offertownship" />
        <activity android:name=".offerlaguna" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyCqDKQ_TW4RVnihJ00Vd8-Y-RAsruKrPiA" />

        <activity
            android:name=".Settings"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        <activity android:name=".offerpopupcard" />
        <activity android:name=".opening" />
        <activity android:name=".petaluma" />
        <activity android:name=".township" />
        <activity android:name=".laguna" />
        <activity android:name=".welcomescreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>